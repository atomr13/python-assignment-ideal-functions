# Python Assignment – Ideal Function Approximation

This repository contains the implementation of a Python assignment focused on
least-squares-based ideal function selection and deviation-based test data mapping.

## Structure
- `src/` – Core implementation
- `data/` – Provided datasets
- `assignment.ipynb` – Execution and visualization

## Usage
Install dependencies:
```bash
pip install -r requirements.txt
